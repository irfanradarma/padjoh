# Copy this file to ".env" for local development (npm run dev).
# These two values are safe to expose in client code.
VITE_SUPABASE_URL=https://cdypfbswmzwvfjoqzykh.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_Yx5dgNbRF6OWd2HqZJ-1nw_EjNLcmH6

# Turns an NPM into an email Supabase Auth accepts: <npm>@npm.app
# This MUST stay identical to the domain in sql/setup.sql (hook_check_npm_whitelist).
VITE_NPM_EMAIL_DOMAIN=npm.app

# Server-only Supabase Edge Function secret for CISA case worksheet decryption.
# The actual private JWK is kept in ignored .env.cisa.local, never in VITE_* or public HTML.
# CISA_CASE_PRIVATE_JWK=<private RSA JWK JSON>
# ANTHROPIC_API_KEY=<server-side API key for draft AI grading>
